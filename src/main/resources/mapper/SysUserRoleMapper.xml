<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paob.tms.mapper.SysUserRoleMapper">
    <select id="selectUserRoles" resultType="com.paob.tms.dto.resp.UserRoleResp">
        SELECT 
            r.id as role_id,
            r.role_name,
            r.role_status
        FROM sys_user_role ur
        LEFT JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
        AND ur.deleted = 0
        AND r.role_status = 1
        ORDER BY r.id ASC
    </select>
</mapper> 