---
description: 
globs: 
alwaysApply: false
---
# Java 项目规则

## 描述

此规则适用于项目内所有 Java 文件，旨在确保代码符合 Java 编码规范，提高代码的可读性和可维护性。

## Globs

**/*.java

## 规则详情

### 缩进

- 使用 4 个空格进行缩进，避免使用制表符。

### 命名规范

- 类名使用大驼峰命名法（PascalCase），例如 `MyClass`。
- 方法名使用小驼峰命名法（camelCase），例如 `calculateSum`。
- 变量名使用小驼峰命名法（camelCase），例如 `myVariable`。
- 常量名使用全大写字母，单词之间用下划线分隔，例如 `MAX_VALUE`。

### 注释

- 为每个类、方法和字段添加 Javadoc 注释，说明其功能、参数和返回值。
- 在复杂代码块添加行注释，解释代码的逻辑。

### 导入语句

- 导入语句应按以下顺序排列：
  1. 标准库导入
  2. 第三方库导入
  3. 本地模块导入
- 不同类型的导入之间用空行分隔。
- 避免使用通配符导入（如 `import java.util.*;`）。

### 代码长度

- 每行代码长度不超过 100 个字符，长语句可以使用括号进行换行。

### 代码结构

- 类中的方法应按照一定的逻辑顺序排列，例如构造方法、公共方法、私有方法等。
- 类的成员变量应在类的顶部声明。

### 异常处理

- 捕获异常后应进行适当的处理，避免简单地打印堆栈跟踪而不做其他操作。
- 对于可能抛出异常的方法，应在方法签名中声明异常。

### 参考文件

@file ../pom.xml