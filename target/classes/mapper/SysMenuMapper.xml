<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paob.tms.mapper.SysMenuMapper">
    <select id="selectMenuList" resultType="com.paob.tms.dto.resp.MenuResp">
        SELECT 
            m.id,
            m.menu_name,
            m.menu_path,
            m.menu_icon,
            m.menu_status,
            m.menu_sort,
            m.parent_id,
            m.updated_by,
            m.updated_time
        FROM sys_menu m
        <where>
            <if test="req.menuName != null and req.menuName != ''">
                AND m.menu_name LIKE '%' || #{req.menuName} || '%'
            </if>
            <if test="req.menuStatus != null">
                AND m.menu_status = #{req.menuStatus}
            </if>
            AND m.deleted = 0
        </where>
        ORDER BY m.menu_sort ASC
    </select>
</mapper> 